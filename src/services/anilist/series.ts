// Season grouping REMOVED (2026-09-19): anime entries are independent
// AniList media entities. Relations now power Related Entries only — see
// src/hooks/useRelatedEntries.ts. This module is kept as a stub so old
// imports fail loudly at build time instead of silently.
export {};
